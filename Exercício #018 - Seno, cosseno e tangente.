'''
Faça um programa que leia um ângulo qualquer e mostre na tela o vaor do seno, cosseno e tangente desse
ângulo.
'''
from math import radians, sin, cos, tan
angulo = float(input('Digite o ângulo que você deseja: '))
sen = sin(radians(angulo))
cos = cos(radians(angulo))
tg = tan(radians(angulo))
print(f'''O ângulo de {angulo} tem o SENO de {sen:.2f}
O ângulo de {angulo} tem o COSSENO de {cos:.2f}
O ângulo de {angulo} tem a TANGENTE de {tg:.2f}''')
